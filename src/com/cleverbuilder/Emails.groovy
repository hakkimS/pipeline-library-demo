#!/usr/bin/env groovy
package com.cleverbuilder

class Emails {
  static def consoleParser() {
                    for(int no in seqno) {
                             if (!Primes.isPrime(no)) {
                             echo "${no} was not prime"
                    }
            }
}
